{
  "mid_dot_helper": "lore_eden.dot_line.Dot / mid_dot",
  "git_subprocess_helper": "lore_eden.git.run_git",
  "git_subprocess_helper_path": "python/lore_eden/git.py",
  "css_token_source": "ts/src/tokens/specs.ts",
  "ungated_globs": {
    "*.md": "prose; there is no markdown linter this repo has agreed to, and a gate nobody agrees with is one people bypass",
    "LICENSE": "verbatim licence text, which must not be edited to satisfy anything",
    ".gitignore": "a list of paths; the gate that would matter is the coverage check itself, which reads the index and so already sees what this excludes",
    "*.json": "configuration and lockfiles; malformed JSON fails the tool that reads it, which is a faster and more specific signal than a gate",
    "*.toml": "configuration; policy/ruff-base.toml is exercised by every ruff run and pyproject.toml by every install and the packaging test",
    "*.yml": "CI workflows and lefthook config; GitHub validates the first on push and lefthook the second on every hook run"
  }
}
